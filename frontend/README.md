# Urus frontend

Vue 3 + Vite + TypeScript + Pinia frontend. It talks to the backend only through `src/api/client.ts`.

```bash
cd frontend
npm install
npm run dev
npm test
npm run build
```

Set `VITE_API_BASE_URL` when the backend is not at `http://127.0.0.1:8000/api`.

