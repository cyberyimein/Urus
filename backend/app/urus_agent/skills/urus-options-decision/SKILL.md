# Runtime skill location

The authoritative `urus-options-decision` Skill is maintained in
`.codex/skills/urus-options-decision/` together with its reference contracts.
`SkillLoader` loads that project-level directory so the developer-facing and
runtime instructions cannot drift apart.
